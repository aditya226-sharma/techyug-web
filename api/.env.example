# Central API Production Environment Variables

# The port to run the server on (automatically set by hosting environments like Render)
PORT=5001

# The allowed CORS origin (e.g. https://techyug.in, https://admin.techyug.in, https://ceo.techyug.in)
# Separate origins with commas
ALLOWED_ORIGINS=
