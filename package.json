{
  "name": "techyug-ecosystem",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix public-site",
    "dev": "npm run dev --prefix public-site",
    "build:all": "npm run build --prefix public-site"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
